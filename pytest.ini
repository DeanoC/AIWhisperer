[pytest]
markers =
    integration: Integration tests
    slow: Slow tests
    performance: marks tests as performance/stability (deselect with '-m "not performance"')
    flaky: marks tests that have isolation issues and may fail when run in full suite

addopts = -W error